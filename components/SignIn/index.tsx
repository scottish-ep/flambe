import className from "classnames";
import React from "react";
import style from "../../styles/SignInStyle/style.module.css"
type Props = {};

export default function SignIn({}: Props) {

  return (
    <div className={`${style.banner_signin} flex flex-row justify-between h-screen w-screen`}>
      
    </div>
  );
}
